name: passline_mobile
description: Passline mobile client.

version: 0.2.0

environment:
  sdk: ">=2.2.0 <3.0.0"

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  items_repository:
    path: items_repository
  user_repository:
    path: user_repository
  firebase_core: ^0.5.0
  cupertino_icons: ^0.1.2
  flutter_bloc: ^6.0.0
  equatable: ^1.2.0
  password_hash: ^2.0.0
  cryptography: ^1.4.1
  local_auth: ^0.6.2+3
  shared_preferences: ^0.5.8
  package_info: ^0.4.1
  http: ^0.12.2

dev_dependencies:
  test: ^1.15.3

