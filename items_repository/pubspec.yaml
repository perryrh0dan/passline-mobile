name: items_repository

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.13.6
  rxdart: ^0.22.6
  equatable: ^1.2.0
  firebase_core: ^0.4.5
